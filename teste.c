#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main ()
{
	char str[] = "2444library_file";
	int number;
	number = atoi(str);
	printf("String = %s\n",str);
	printf("Number = %d\n",number);

	return 0;
}
